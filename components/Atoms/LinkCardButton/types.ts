import { CardSizes } from 'components/DefaultCard/types';

export type LinkCardButtonProps = {
  name: string;
  url: string;
  size?: CardSizes;
};

export type LinkButtonProps = {
  size?: CardSizes;
};
