import { motion } from 'framer-motion';
import styled from 'styled-components';

export const CarousalInfoContainer = styled(motion.div)`
  display: flex;
  align-items: center;
  gap: 16px;
  width: 100%;
`;
