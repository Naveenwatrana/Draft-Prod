import styled from 'styled-components';

export const ContentContainer = styled.span`
  display: flex;
  flex-direction: column;
  flex: 1%;
  button:last-child {
    position: absolute;
    bottom: 24px;
    line-height: 20px;
  }
`;
