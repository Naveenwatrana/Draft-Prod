import { IProjectValues } from 'pages/pro/components/Projects/types';

export type ProjectBarProps = {
    project: IProjectValues;
    lineWidth: number;
    timeLine: string;
    gridGap: number;
};
